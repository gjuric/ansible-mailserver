user        = {{ mail_db_username }}
password    = {{ mail_db_password }}
hosts       = {{ mail_db_host }}
port        = {{ mail_db_port }}
dbname      = {{ mail_db_name }}
query       = SELECT alias_destination.destination FROM alias_destination LEFT JOIN alias ON alias_destination.alias = alias.address LEFT JOIN domain ON alias.domain = domain.name WHERE alias_destination.alias='%s' AND alias.active=1 AND domain.active=1 AND domain.backup_mx=0
