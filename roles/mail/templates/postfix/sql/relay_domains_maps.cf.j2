user        = {{ mail_db_username }}
password    = {{ mail_db_password }}
hosts       = {{ mail_db_host }}
port        = {{ mail_db_port }}
dbname      = {{ mail_db_name }}
query       = SELECT name FROM domain WHERE name='%s' AND backup_mx=1 AND active=1
