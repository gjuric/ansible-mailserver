---
- hosts: all
  sudo: true
  vars_files:
    - settings.yml
  roles:
    - { role: common, tags: common }
    - { role: mail, tags: mail }
    - { role: ntp, tags: ntp }
    - { role: unbound, tags: unbound }
    - { role: timezone, tags: timezone }
